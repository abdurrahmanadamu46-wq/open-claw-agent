# Compliance Launch Profile

Use `icp_launch_profile.template.json` as the single place to collect offline launch metadata:

- legal entity
- domain owner
- operator contact
- ICP filing references
- payment merchant references

This file is intentionally placeholder-first so the codebase can remain launch-ready even before
the legal and domain teams finish the offline material handoff.
