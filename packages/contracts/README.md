# Contracts Package

Single source of truth for:
- OpenAPI surface between web -> backend -> ai-subservice
- Event schemas for queue, dlq, replay, and edge telemetry
- Shared DTO schemas used by CI contract checks

Status: baseline v1 (non-breaking). Existing runtime interfaces stay unchanged.
